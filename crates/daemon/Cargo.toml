[package]
name = "daemon"
version = "0.1.0"
edition = "2021"

[dependencies]
core-protocol = { path = "../core-protocol" }
tokio = { version = "1.38", features = ["full"] }
uuid = { version = "1.8", features = ["serde", "v4"] }
